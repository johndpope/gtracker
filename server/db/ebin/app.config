%% rotating log and errors
[
   {
      sasl, [
            %% minimise shell error logging
            {sasl_error_logger, false},
            %% the log file directory
            {error_logger_mf_dir,"logs" },
            %% # bytes per logfile
            {error_logger_mf_maxbytes,10485760}, % 10 MB
            %% maximum number of
            {error_logger_mf_maxfiles, 10}
        ]
   },
   {
      gtracker_db, [
            {mds_server, [{root_dir, "logs"}]},
            {mds_logger, [{log_level, debug}, {enable_alarm, false}]},
            {sup_mod,    gtracker_sup},
            {servers, [
%                  {
%                     gtracker_mysql,
%                     [
%                        {mds_server, [{working_dir, mysql}]},
%                        {self, [{dbhost, "localhost"}, {dbport, 3306}, {dbuser, "gtuser"}, {dbpasswd, "Meech20h"}, {dbname, "gtracker"}]}
%                     ]
%                  },
                  {
                     gtracker_mnesia,
                     [
                        {mds_server, [{working_dir, mnesia}]},
                        {self, [{as_track_node, true}]}
                     ]
                  },
                  {
                     gtracker_alarm,
                     [
                        {self, [{to, "dmitryme@gmail.com"}, {subject, "gTracker: "}]}
                     ]
                  }
               ]
            }
         ]
   }
].
